function artificialPotentialField(pos,goal,tol, obs, params)
% creating a function based on an implementation on file exchange, needs
% change to account for multi obs 
x0 = pos(1);
y0 = pos(2);
theta = pos(3);

xg = goal(1);
yg = goal(2);


zeta = params(1);
eta = params(2);
dstar = params(3);
Qstar = params(4);

  
end
